package syntax;

public class Ternary {
    public static void main(String[] args) {

        boolean value = false;

        String resolution = value ? "Verdadeiro" : "Falso";
        System.out.println(resolution);

    }
}
